# A test project for testing the interaction between JavaScript and Rust for signing and verification processes.

### Prerequisites
* nodejs environment
* rust environment

### Tests
* cd rs & cargo test -- --nocapture
* cd js & node test.mjs